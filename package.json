{
  "name": "env-creator",
  "version": "1.0.0",
  "description": "Environment variable generator CLI",
  "type": "module",
  "bin": {
    "env-creator": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint .",
    "build": "esbuild src/index.js --bundle --platform=node --format=esm --minify --outfile=dist/index.js --banner:js=\"#!/usr/bin/env node\""
  },
  "keywords": [],
  "author": "Kyrylo Voronoi",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.3",
    "globals": "^17.4.0"
  }
}
